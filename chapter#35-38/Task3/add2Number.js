var base=prompt("Enter 1st number to add");
var per=prompt("Enter 2nd number to add");
var ans;
function demo1(base, per){
ans= parseInt(base) + parseInt(per);
document.write(ans);
}
demo1(base, per);